{
	"name": "@afroinvest/server",
	"version": "0.1.0",
	"private": "true",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"dev": "nodemon index.js",
		"prestart": "rm -rf dist && npm i && cp -r ../client/dist/ .",
		"start": "node index.js"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.3.1",
		"express": "^4.18.2",
		"mongoose": "^8.1.0",
		"morgan": "^1.10.0"
	},
	"devDependencies": {
		"nodemon": "^3.0.3"
	}
}
